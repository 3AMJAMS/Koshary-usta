A one-line paper confirmation that springs up over the dock — a purchase landed, a batch finished, the chickpeas are soaking.

```jsx
<Toast open={!!msg}>{msg}</Toast>
```

Pill-shaped paper on ink, centred, never more than one at a time, never interactive. Use it for things the player should notice but not act on; anything that needs a decision belongs in a `Panel`.
